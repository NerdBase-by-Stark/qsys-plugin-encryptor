# Python / PyInstaller build artifacts
__pycache__/
*.pyc
.venv/
venv/
build/
dist/
*.egg-info/

# PowerShell GUI runtime config (created next to the script at first run)
qplug-encryptor.config

# QSC encryption tool — fetched at build time from qsys-plugins/PluginEncryptionTool,
# bundled into the .exe, never committed here.
vendor/
